management:
  tracing:
    enabled: false
    sampling:
      probability: 1.0
  otlp:
    enabled: false
    tracing:
      endpoint: http://localhost:4318/v1/traces
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      access: unrestricted