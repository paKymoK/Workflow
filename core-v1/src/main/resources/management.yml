management:
  tracing:
    enabled: false
    sampling:
      probability: 1.0
  otlp:
    enabled: false
    tracing:
      endpoint: http://localhost:4318/v1/traces
  health:
    redis:
      enabled: false