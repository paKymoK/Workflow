apiVersion: 1

contactPoints:
  - orgId: 1
    name: error-webhook
    receivers:
      - uid: error-webhook-1
        type: webhook
        settings:
          # TODO: replace with the real endpoint once decided, then `docker restart grafana`
          url: https://REPLACE_ME.example.com/webhook
          httpMethod: POST
