// Superseded by Phase 7: see service/DepartmentService.java. Stubbed rather than deleted (the
// environment this was authored in couldn't run `rm`) — no longer referenced anywhere in the
// codebase.
