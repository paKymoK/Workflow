package com.takypok.employeeservice.model.entity;

/** Archiving, not deletion — documents are org-official records worth keeping for audit/history. */
public enum DocumentStatus {
  ACTIVE,
  ARCHIVED
}
