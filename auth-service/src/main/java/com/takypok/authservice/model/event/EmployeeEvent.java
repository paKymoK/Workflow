// Superseded by Phase 7: auth-service no longer consumes employee.events (see
// EmployeeEventConsumer). Stubbed rather than deleted (the environment this was authored in
// couldn't run `rm`) — no longer referenced anywhere in the codebase.
