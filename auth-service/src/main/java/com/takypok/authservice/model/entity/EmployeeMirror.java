// Superseded by Phase 7: name/email are now auth-service-owned data (see Userinfo), not a
// Kafka-fed mirror of employee-service's copy. Stubbed rather than deleted (the environment this
// was authored in couldn't run `rm`) — no longer referenced anywhere in the codebase.
