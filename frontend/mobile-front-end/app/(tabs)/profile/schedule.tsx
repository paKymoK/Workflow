import { ScreenStub } from '@/src/components/ScreenStub';

export default function ScheduleScreen() {
  return <ScreenStub title="Work Schedule / Shift" description="Shift schedule calendar goes here." />;
}
