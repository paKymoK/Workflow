{
  "installCommand": "cd .. && npx --yes pnpm@10.33.2 install --no-frozen-lockfile",
  "buildCommand": "cd .. && npx --yes pnpm@10.33.2 --filter workflow-front-end build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
