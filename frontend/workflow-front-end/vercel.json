{
  "installCommand": "corepack enable pnpm && cd .. && pnpm install --frozen-lockfile",
  "buildCommand": "cd .. && pnpm --filter workflow-front-end build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
