{
  "installCommand": "npm install -g pnpm@10.33.2 && cd .. && pnpm install --frozen-lockfile",
  "buildCommand": "cd .. && pnpm --filter workflow-front-end build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
